# Vacío = paths relativos (/api/...) — funciona con el proxy de Vite en dev
# (ver vite.config.ts) y con un deploy same-origin detrás de Cloudflare
# Tunnel. Solo hace falta setearlo si el frontend y el backend corren en
# dominios distintos.
VITE_API_URL=
